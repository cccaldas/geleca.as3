package com.geleca.as3.test.date
{
	import com.geleca.as3.date.DateTime;
	import com.geleca.as3.date.Month;
	import com.geleca.as3.test.Assert;

	public class MonthDaysTest extends Object
	{	
		public function MonthDaysTest()
		{
			super();
		}
		
		public function run():void
		{
			/*for each (var month:Month in DateTime.now.months)
				trace("MonthDaysTest::run()", month, month.month, month.days);
				
			Assert.assertEquals(DateTime.now.months[1].month, 					Month.FEBRUARY);
			Assert.assertEquals(DateTime.now.months[10].month, 					Month.NOVEMBER);
			Assert.assertEquals(DateTime.now.getMonth(Month.DECEMBER).days, 	31);*/
		}
	
	}

}