package Geleca.Util
{
	public class DataProviderUtil
	{
		
		public function DataProviderUtil() 
		{
			
		}
		
		/*public static function randomize(provider:DataProvider):void 
		{
			for (var i:int = 0; i < provider.length; i++) 
			{
				provider.addItemAt(provider.removeItemAt(i), Math.abs(Math.random() * provider.length - 1));
			}
		}
		
		public static function convertFromVector(vector:*):DataProvider 
		{
			var dp		:DataProvider = new DataProvider();
			var length	:uint = vector.length;
			
			for(var i:int = 0 ; i < length ; i++)
			{
				dp.addItem(vector[i]);
			}
			
			return dp;
		}*/
		
	}

}